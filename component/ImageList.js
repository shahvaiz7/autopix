import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function ImageList({photo}) {
  return (
    <View>
      <Text>ImageList {photo}</Text>
    </View>
  )
}

const styles = StyleSheet.create({})